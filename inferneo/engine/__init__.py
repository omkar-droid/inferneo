"""Engine control plane.

Import submodules directly (e.g. ``inferneo.engine.scheduler``) — this
package deliberately avoids eager imports so the scheduler and request
types stay importable without torch.
"""
